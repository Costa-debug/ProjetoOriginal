<Page x:Class="ProjetoIntegradorOficial.Estoque"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjetoIntegradorOficial"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Estoque">

    <Viewbox Stretch="Fill">
        <Grid Width="800" Height="450">
            <Image HorizontalAlignment="Left" Height="467" VerticalAlignment="Top" Width="808" Source="/jasme.png"/>
            <DataGrid x:Name="dtEstoque" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" FontSize="18" Margin="0,87,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Produto" Binding="{Binding Produto}" Width="*"/>
                    <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="Auto"/>
                    <DataGridTextColumn Header="Preço Unitário" Binding="{Binding Preco}" Width="Auto"/>
                    <DataGridTemplateColumn Header="Ações" Width="Auto">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remover"
            Width="100"
            Height="30"
            Foreground="Black"
            Cursor="Hand"
            Click="Remover_Click"
            BorderThickness="0">
                                    <Button.Background>
                                        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                            <GradientStop Color="White"/>
                                            <GradientStop Color="#FFFFF157" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Button.Background>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
                
            <Button Content="Voltar" HorizontalAlignment="Left" Margin="31,24,0,0" VerticalAlignment="Top" Height="44" Width="114" Background="#FFFF5B5B" FontSize="16" Click="Button_Click"/>

        </Grid>
    </Viewbox>
</Page>
